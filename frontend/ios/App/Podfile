# Capacitor usa Swift Package Manager (SPM), não CocoaPods
# Este Podfile existe apenas para evitar erros no Codemagic
# O projeto não usa CocoaPods - todas as dependências são via SPM

platform :ios, '13.0'

# Usar o projeto diretamente (não workspace)
project 'App.xcodeproj'

# Não há dependências - projeto usa SPM
target 'App' do
  # Vazio - sem dependências CocoaPods
end

# Instalar sem criar workspace (já que não usamos CocoaPods)
install! 'cocoapods', :deterministic_uuids => false
